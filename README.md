# prodlogger

Express CRUD app for tracking project release logs. Vue client is not finished and probably won't be because I have other priorities.

run server:
```bash
$ cd server
$ pnpm i
$ pnpm dev
```

run client:
```bash
cd client
pnpm i
pnpm dev
```
